# Clean Agent (modern wording)

Use the search tool when the answer depends on information not in the conversation.
Report every finding with a confidence level and severity; a downstream step filters.
P0 gate: planner intent is NEVER expanded without approval (mechanically checked at Stage 0).
